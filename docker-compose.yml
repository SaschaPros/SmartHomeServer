services:
  smarthome:
    container_name: server
    hostname: smarthome
    build: ./
    restart: always
    environment:
      - SERVER_PORT=${PORT:-3000}
    ports:
      - ${PORT:-3000}:${PORT:-3000}
